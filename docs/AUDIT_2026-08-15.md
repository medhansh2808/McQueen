# AUDIT — 2026-08-15 (lab day: training chain, deploy, app build, transport full-loop)

**Scope rule:** only real-machine results from the 2026-08-15 lab session (RTX 4090
`192.168.0.132`, Jetson Nano `192.168.55.1` via USB, laptop on same WiFi). No clock
timestamps — ordering/sequence only. Status legend: VERIFIED · BLOCKED · FIXED · NOTED.

---

## 1. VERIFIED — SSH/auth + asset staging
- SSH auth OK on both machines (transient askpass helpers, removed after use).
- RTX public IP still `14.139.108.62` (curl from RTX, live).
- Lerobot env on RTX = 0.6.0 (matches the LeRobot converter).
- scp: 5 converted rehearsal datasets (305 MB) + PPGeo ckpt (84 MB) + `mcqueen_ml`
  package → `/home/junior/mcqueen_run/` on the RTX.

## 2. VERIFIED — training-pipeline pre-flight (smoke) on the RTX
- `smoke_train_batch.py --device cpu`: PASS. 630 samples / 6 episodes, batch
  collate, forward `(8, 6, 3, 224, 224) -> (8, 2)`, checkpoint save/reload contract
  OK. RAM guard saw 53.2 GB available.

## 3. VERIFIED — rehearsal training (CPU, no GPU touch)
- GPU was occupied by an unrelated long-running training job (20.3 GB VRAM, 82%
  util) — per the shared-machine rule it was NOT touched, killed or disturbed.
- Training ran on CPU: 20/20 epochs, tiny backbone, batch 16. Checkpoint:
  `/home/junior/mcqueen_run/rehearsal_temporal.pt` (36 MB, best_val 3.02).
- Checkpoint contract keys: model_state_dict / backbone / config / action_names
  (servo_angle_deg, motor_pwm) / stats (servo+pwm mean/std) / history 6 /
  image_size 224 / epochs / seed / train_loss / val_loss.

## 4. VERIFIED — inference chain on the RTX with the REAL checkpoint
- `test_checkpoint_inference.py` with real checkpoint: 10/10 PASS (denorm
  matches manual raw*std+mean; servo + pwm clamps enforced).
- `test_inference_rtx.py`: 18/18 PASS (default random-tiny path intact; CUDA
  single-frame infer measured 444.75 ms — GPU path proven when free).
- `inference_rtx.py --checkpoint <real ckpt> --device cpu`: loads backbone=tiny
  history=6 input=224x224; outputs servo 89.0 / pwm 4 (near-center, sane);
  infer avg **6.59 ms/frame** on CPU.

## 5. VERIFIED — Jetson deploy (repo is source of truth)
- Repo copies pushed over stale/older copies on the Jetson clone (`61a3c91`):
  `gst_jetson_rtp_wan.py` (26,464 B), `kachow_probe.py` (5,526 B),
  `measure_true_path_rtt.py`.

## 6. VERIFIED — Android app build
- Gradle 9.4.1 (SHA-verified dist) + JDK 17: `assembleDebug` BUILD SUCCESSFUL in
  27 s → `apps/android/KachowV8/app/build/outputs/apk/debug/app-debug.apk`.

## 7. VERIFIED — true-path RTT probe tool (mechanics)
- LAN run laptop → RTX `192.168.0.132:5955`: 10/10 received, min 1.96 / avg
  3.36 / p95 3.84 / max 4.37 ms, jitter 0.57 ms. Listener echoed all 10 (log
  `rtt_listener.log`: seq=0..9 bytes=52).
- WAN ping Jetson → `14.139.108.62`: 0% loss, min 45.4 / avg 53.8 / max 62.3 ms
  (WAN path alive, ~54 ms RTT).

## 8. FIXED — full WAN loop down: dead cloudflared + stale broker URL (NOT port-forward)
- The loop was down because the **cloudflared tunnel on the RTX was dead** (log:
  `Tunnel server stopped`) and the URL file pointed at the dead host
  `unfortunately-wrestling-kim-traveller.trycloudflare.com` (F8-class stale URL).
- The earlier "campus port-forward is missing" theory was **WRONG**: the pipeline
  binds an ephemeral port (`0.0.0.0:0`) + STUN-punches; campus NAT is
  endpoint-independent so the punch works with NO forward. Evidence: receiver
  public `14.139.108.62:49995` (then `:41820`, `:53591`), Jetson public
  `152.59.109.96:59856`, peer-to-peer UDP media after punch.
- Fix: fresh `cloudflared` → new URL `https://carlo-booth-austin-pics.trycloudflare.com`,
  URL file refreshed, signaling verified from the Jetson (`{"ok": true ...}`).
- Status: **FIXED** (verified live).

## 9. VERIFIED — full WAN loop (punched UDP, real machines)
- Jetson sender (hotspot WAN) → punched UDP → RTX receiver → control back.
- Run (300 kbps): sender `CTRL_RX n=960`, **LAT_p50=276.5 ms / LAT_p95=486.7 ms**;
  receiver `RTP_RX pkts=4530 meta=1069 frames_rx=1072`, fps 18.3,
  assoc 941/1019 (~94%), infer_avg 0.12 ms. Reproduces 2026-08-14 (~287 ms @150 kbps).
- Media/control flow DIRECT peer-to-peer over UDP — cloudflared is not on the path.

## 10. VERIFIED — bitrate A/B (150/300/400 kbps x2, 25 s observe each)
- 5/6 runs real data; 1 transient websocket 530 (cloudflared blip) → manual 400 kbps
  rerun completed the matrix. Full table + logs: `docs/evidence/2026-08-15/ab/`
  and `docs/evidence/2026-08-15/REPORT.md`.
- Summary: median loop latency stable ~270-280 ms across bitrates (fps 18-22,
  assoc ~94-96%). Control return + `LAT_p50` present in every data run.
- A/B script itself: 3 launch failures diagnosed and fixed (see ERROR_LOG entry D).

## 11. FIXED — probe script Python 3.6 compatibility
- `measure_true_path_rtt.py` crashed on the Jetson (`time.monotonic_ns` needs
  Python 3.7; Jetson ships 3.6) → replaced with 3.6-safe `int(time.monotonic()*1e9)`
  + per-probe verbose progress. py_compile OK; LAN probe verified after fix.

## 12. VERIFIED — Jio 5G full-loop test: NO improvement (worse tail)
- Jetson+camera+phone walked outside on a power bank to a 5G/5G++ spot; RTX + laptop
  stayed in the lab; resilient autostart wrapper ran the sender through the whole
  walk (0 restarts). RTX: 49,369 frames, fps 20.6, assoc 95.3%, ctrl_sent 46,094.
- p50 trajectory: best ~223-224 ms at the desk on stable 4G; 5G++ spot window
  ~231-237 ms (≈ stable 4G); cumulative end 257.7 ms; **p95 worsened to ~1239 ms**
  from 5G↔5G++↔4G mode-shifting. **5G does not beat stable 4G on this phone.**
- En-route bugs FIXED: Jetson `/tmp` is tmpfs (wiped on reboot → autostart moved to
  `/home/sravjti/mcqueen_5g/`); `mcqueen-edge.service` holds `/dev/video0` at boot
  (stopped for the test, restored after); an unanchored pkill inside a cleanup ssh
  killed the cleanup shell (F7-class → precise PID kills).
- Evidence: `docs/evidence/2026-08-15/5g/` (REPORT.md + sender/receiver logs).
  Cleanup: wrapper+cron removed, `/home/sravjti/mcqueen_5g/` deleted, edge service
  active, RTX receiver stopped, tunnel left up.

---
**Net:** full train → checkpoint → deploy → inference chain verified end-to-end on
real hardware, AND the transport evidence is complete: WAN full loop at
~270-280 ms p50 across bitrates over direct punched UDP, with the dead-tunnel
root cause fixed and documented. Remaining: PPGeo training (waits for the
neighbor's GPU job to finish) and the brokerless-cloudflared future item.
