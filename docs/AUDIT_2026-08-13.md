# AUDIT — McQueen Lab Day 2026-08-13

Complete audit of the 2026-08-13 lab session (the WAN/"server thing" work),
written 2026-08-13 evening for the record. Every problem from the day is listed
with its fix status. Machine-state evidence: `docs/evidence/2026-08-13-lab-pull/`
(78-file snapshot), `docs/evidence/2026-08-13-wan-pipeline-errors.txt`
(21-entry error log). Recordings: `data/lab_pull_20260813/` (gitignored).

> NOTE: an offline home-debug session (2026-08-13 evening) produced code-level
> fixes + tests for the still-open items. Per project rule, those fixes are
> intentionally NOT pushed yet — they are committed to the repo only after the
> next hardware test proves them. See `.mcqueen/` state files for the working
> record.

## What got accomplished

1. **Agent system** — AGENTS.md + `.mcqueen/` durable context committed and pushed.
2. **WebRTC diagnosed as dead on the Jetson** (GStreamer 1.14.5/libnice 0.1.14
   cannot gather srflx candidates → cannot cross the phone-hotspot CGNAT).
   Replaced with **raw RTP over direct NAT-punched UDP**, using the same
   broker + cloudflared rendezvous that proved 60/60 ACKs (p50 43.1 ms) for the
   control path on 2026-08-11.
3. **New transport code** — Jetson Sends (`gst_jetson_rtp_wan.py`, cv2 + x264
   software + manual packetization), RTX receiver (`gst_rtx_rtp_receiver.py`,
   rtph264depay + decodebin + CUDA dummy inference + exact-frame CTRL return),
   one-shot lab runner (`run_rtp_wan_test.sh`).
4. **Complete evidence pull** — every deployed script, log, pid, the
   22,209-line NameError flood, 15 receiver runs, broker/cloudflared state,
   recordings (gitignored).
5. **All of it committed and pushed to GitHub.**

## Problems faced — full list with fix status

### Architecture / transport

| # | Problem | Status |
|---|---|---|
| 6 | **THE BIG ONE:** WebRTC ICE FAILED — Jetson gathers zero srflx candidates, stuck in CHECKING forever (old GStreamer/libnice limitation) | **WORKAROUNDED, not fixed** — abandoned WebRTC for NAT-punched raw RTP/UDP. WebRTC stays broken on the Jetson stack (needs newer GStreamer/libnice or TURN; not needed for the <100 ms proof) |
| 8 | College network blocks Tailscale/ZeroTier mesh | CLOSED (external constraint) |
| 7 | STUN TCP test gave misleading "UNREACHABLE" (STUN is UDP) | RESOLVED (raw STUN UDP test: success, 69 ms) |

### SSH / operational friction

| # | Problem | Status |
|---|---|---|
| 1 | SSH password prompt fails in agent shell (no TTY/askpass) | RESOLVED (transient askpass helper, deleted after) |
| 2/3/4 | SSH times out starting background processes (broker, cloudflared) — looked like failures | RESOLVED (start in one call, verify separately — false alarms, both ran fine) |
| 5 | RTX receiver reported "DEAD" — empty pid file | RESOLVED (use pgrep, not pid files) |
| 12 | RTX unreachable after NAT punch — stale ARP on laptop | RESOLVED (flush ARP; hardware fine) |
| 18 | SSH rejected for ~10 min — **wrong username** (kartik vs sravjti) | RESOLVED (correct user is `sravjti@192.168.55.1`) |

### Sender-side code bugs (video path)

| # | Problem | Status |
|---|---|---|
| 9 | One capture entry consumed per RTP packet instead of per frame | RESOLVED pre-deployment (frame boundary via RTP ts change; `test_rtp_association.py` PASS) |
| 16 | Sender crashed after NVENC init — use-after-free in RTP probe | FIXED (copy payload before unmap + exception-proof probes) — deployed to Jetson |
| 20 / F1 | **NameError flood `% 30 < n` (n undefined)** — 22,209 identical errors, froze rtp_ts at 0 → RTX merged all frames into 1 AU (frames_rx=0) | **FIXED on laptop copy (`% 30 == 0`, committed in `6698d41`), NOT redeployed to Jetson** — redeploy next lab via `run_rtp_wan_test.sh` |
| 19 | **Sender stalls after ONE frame** (30 pkts/1 meta then 0% CPU) — NVENC-era camera/decoder chain quirk, not transport | **OPEN** at day end; root cause pinned in the follow-up home-debug session (fix pending hardware test) |
| 10 | Rendezvous announce window too short (late peer never learns) | RESOLVED (continuous announce 0.35s + 60s wait) |
| 11 | Wait loop punched its own public address | RESOLVED (cleanup) |
| 13 | Test harness bugs (TypeError, bad unpacking) | RESOLVED (rewrote as unit test, PASS) |
| 14 | Laptop lacks H.264 GStreamer plugins | CLOSED (laptop not the runtime; target machines verified) |
| 17 | RTX receiver died "Jetson UDP candidate missing" | RESOLVED (cascade of #16; restart both) |

### Receiver-side

| # | Problem | Status |
|---|---|---|
| 21 / F2 | RTX receiver lab16 crashed: `ModuleNotFoundError: websocket` (system python) | RESOLVED operationally — must use `gst-webrtc-venv/bin/python` (venv verified, F6); encoded in `run_rtp_wan_test.sh` |
| F3 | lab15: depay stuck "waiting for start" (FU-A S=1/E=0 + AUD NAL 9) | Explained (old NVENC-sender artifact; new sender drops AUD) — confirmation pending next lab |
| F4/F5 | Deployed Jetson sender older than laptop copy; buggy 584-line copy was on the Jetson (`/tmp`, md5-identical to laptop pre-fix) | Documented; fixed copy must be redeployed |

### Pre-existing repo issues (not caused this day — mostly fixed)

| Problem | Status |
|---|---|
| `test_jetson_http_server.py` — stale, expected old "OAK-D/WebRTC" HTML | **FIXED** (commit `6698d41`) |
| `test_jetson_edge_app.py` — failed at import (recording False) | **FIXED** (commit `6698d41`) |
| `laptop_lab_preflight.sh` — checked obsolete steering mapping (-1000→45°) | **FIXED** (commit `6698d41`) — now -1000→115°, matching code + unit test |
| `test_temporal_policy_v2.py` — needs torch | **STILL BLOCKED** (env: torch lives in `mcqueen-laptop` env, not system python3.10) |

## Current state (verified at audit time)

- Startup check: **35 PASS / 0 FAIL**.
- Test suite: **18 passed**, 1 env-blocked collector (torch).
- Local == GitHub (both at `6698d41`); only untracked item is
  `context stuff for understanding the mcqueen project/` (intentionally local,
  DECISION 008).
- Recordings mirrored to `data/lab_pull_20260813/` (gitignored).

## Still open (blocked until next lab)

1. **#19 sender stall after 1 frame** — NVENC-era camera chain; offline home
   analysis pinned the cause; hardware proof pending.
2. **Q2b** — verify rtp_ts advances + frames_rx > 0 end-to-end with the fixed
   sender + venv receiver (`run_rtp_wan_test.sh`).
3. **Q1** — motor-PWM label path live health (needs phone + KACHOW at lab).
4. **Q4** — RTX env readiness (`rtx4090_preflight_v2.sh`), **Q3** broker host,
   **Q7** network matrix.

See `.mcqueen/OPEN_QUESTIONS.md` for the full question ledger.
