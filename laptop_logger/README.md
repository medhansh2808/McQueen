# laptop logger

`phone_logger_server.py` listens on tcp port 8080

it creates logging sessions under `~/KachowDatasets`

when the experimental goal 2 uploader is active each sample can contain rgb depth detections and the actual motor pwm and servo angle
